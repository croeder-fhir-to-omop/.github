# croeder-fhir-to-omop — project CLAUDE.md

Org-level project rules, shared across all croeder-fhir-to-omop repos. Pulled into each repo's own CLAUDE.md via an `@` import so the rules travel with every repo.

## Source of truth
- Respect files that are the source of truth and do not modify them — including OMOP DDL files like `OMOPCDM_duckdb_5.4_ddl.sql`.

## HL7 FHIR IG work
- Use the bonfhir docker container to do IG builds.
